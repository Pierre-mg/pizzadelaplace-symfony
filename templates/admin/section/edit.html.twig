{% extends 'base_admin.html.twig' %}

{% block title %}Modifier section{% endblock %}

{% block body %}

<div class="container">

    <h1>Modification</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-warning">Modifier</button>
    {{ form_end(form) }}

    <a href="{{ path('section_index') }}">Retour</a>

</div>

{% endblock %}
